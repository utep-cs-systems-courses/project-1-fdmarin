#include <stdlib.h>
#include <stdio.h>
#include "tokenizer.h"

int main(){
  int condition = 1;

  while(condition == 1) {
    printf("> my fancy interface thingy ");
    fgets(word, 1000, stdin);
  }

  return 0;
}

